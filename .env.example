# Server-side GitHub token for secure GraphQL requests.
# Safe for Vercel environment variables because it never reaches browser code.
GITHUB_TOKEN=

# Optional public canonical app URL for share links.
# If omitted, the app falls back to the current deployed origin.
VITE_PUBLIC_APP_URL=https://github-wrapped-liart.vercel.app/
